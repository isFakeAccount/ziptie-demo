[project]
name = "ziptie-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ziptie",
]

[tool.uv.sources]
ziptie = { path = "../ziptie", editable = true }
